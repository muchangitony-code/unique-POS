(function(){'use strict';const retiredRoute='quotations';const retiredActions=new Set(['open-quotation-modal','edit-quotation','print-quotation','download-quotation','email-quotation','convert-quotation','delete-quotation']);function redirect(){if(String(location.hash||'').replace(/^#/,'').split('?')[0]===retiredRoute)location.hash='dashboard';}function clean(root=document){root.querySelectorAll('[data-route="quotations"]').forEach(el=>el.remove());root.querySelectorAll('[data-action]').forEach(el=>{if(retiredActions.has(el.getAttribute('data-action')))el.remove();});const title=document.getElementById('pageTitle');if(title&&/^quotations?$/i.test(title.textContent.trim()))location.hash='dashboard';}redirect();const observer=new MutationObserver(()=>{redirect();clean();});observer.observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',event=>{const target=event.target.closest('[data-route="quotations"],[data-action]');if(!target)return;const action=target.getAttribute('data-action');if(target.getAttribute('data-route')==='quotations'||retiredActions.has(action)){event.preventDefault();event.stopImmediatePropagation();redirect();}},true);document.addEventListener('DOMContentLoaded',()=>{redirect();clean();});clean();})();