(()=>{'use strict';let loading=false,modalBusy=false;const route=()=>String(location.hash||'').replace(/^#/,'').split('?')[0]==='inventory';const auth=()=>{const t=localStorage.getItem('uniquepos.token');return t?{Authorization:'Bearer '+t}:{}};const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const qty=x=>Number(x.quantity_on_hand??x.current_stock??x.opening_stock??x.stock_quantity??x.stock??x.quantity??0)||0;const low=x=>Number(x.reorder_level??x.minimum_stock??x.effective_reorder_level??0)||0;let cached=[];const branch=()=>Number(document.getElementById('branchSelect')?.value)||1;async function products(){const b=branch(),urls=['/api/products?branchId='+encodeURIComponent(b)+'&catalogue=1','/api/products?catalogue=1','/api/v3/inventory/products?branchId='+encodeURIComponent(b)];for(const u of urls){try{const r=await fetch(u,{headers:{...auth(),'X-UniquePOS-Bypass-V3':'1'},cache:'no-store'});if(!r.ok)continue;const j=await r.json();const a=Array.isArray(j)?j:(j.products||j.data||j.items||[]);if(Array.isArray(a)&&a.length){cached=a;return a}}catch(_){}}return cached}function put(label,value){document.querySelectorAll('.kpi-card').forEach(c=>{const l=c.querySelector('.kpi-card__label');if(l&&l.textContent.trim().toLowerCase()===label){const v=c.querySelector('.kpi-card__value');if(v)v.textContent=value;}})}function table(ps){const root=document.getElementById('viewRoot');if(!root)return;let body=root.querySelector('tbody');if(!body)return;body.innerHTML=ps.slice(0,500).map(x=>{const q=qty(x),r=low(x),status=q<=0?'Out of Stock':r>0&&q<=r?'Low Stock':'In Stock';return '<tr><td>'+esc(x.name||x.product_name||x.productName||x.item_name||'Unnamed product')+'</td><td>'+esc(x.sku||x.product_code||x.code||'')+'</td><td>'+q+'</td><td>'+status+'</td></tr>';}).join('')||'<tr><td colspan="4">No products found.</td></tr>'}const productOptions=ps=>'<option value="">Select product</option>'+ps.map(x=>'<option value="'+esc(x.id??x.product_id??'')+'">'+esc(x.name||x.product_name||x.productName||x.item_name||'Unnamed product')+(x.sku||x.product_code?' ('+esc(x.sku||x.product_code)+')':'')+'</option>').join('');function row(ps){return '<div class="inventory-receive-row" style="display:grid;grid-template-columns:minmax(240px,1fr) 140px 44px;gap:12px;margin:10px 0"><select class="ir-product" required>'+productOptions(ps)+'</select><input class="ir-qty" required type="number" min="0.001" step="any" placeholder="Quantity"><button type="button" class="btn btn-secondary ir-remove" title="Remove">×</button></div>'}async function receive(productId,quantity,reference,notes){const payload={productId:Number(productId),product_id:Number(productId),quantity:Number(quantity),branchId:branch(),branch_id:branch(),reference:reference||'',reason:notes||'',notes:notes||''};const r=await fetch('/api/v3/inventory/receive',{method:'POST',headers:{...auth(),'Content-Type':'application/json'},body:JSON.stringify(payload)});const j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error||j.message||'Stock receipt failed');return j}async function enhanceReceive(){if(modalBusy)return;const modal=document.getElementById('modalOverlay'),title=document.getElementById('modalTitle')?.textContent||'';if(!modal||modal.classList.contains('hidden')||!/receive stock/i.test(title)||document.getElementById('inventoryReceiveEnhanced'))return;modalBusy=true;try{const ps=await products();if(!ps.length)return;const body=document.getElementById('modalBody'),subtitle=document.getElementById('modalSubtitle');if(!body)return;if(subtitle)subtitle.textContent='Receive one or multiple existing products into stock.';body.innerHTML='<form id="inventoryReceiveEnhanced" class="stack-form"><div id="irRows">'+row(ps)+'</div><button type="button" class="btn btn-secondary" id="irAddRow">+ Add another product</button><div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:16px"><label>Reference<input id="irReference" value="GRN-001"></label><label>Notes<input id="irNotes"></label></div><div id="irStatus" class="inline-message hidden"></div><div class="modal-actions" style="margin-top:20px"><button class="btn btn-primary" id="irSubmit" type="submit">Receive Stock</button></div></form>';const rows=document.getElementById('irRows');document.getElementById('irAddRow').onclick=()=>rows.insertAdjacentHTML('beforeend',row(ps));rows.onclick=e=>{if(e.target.closest('.ir-remove')){const all=rows.querySelectorAll('.inventory-receive-row');if(all.length>1)e.target.closest('.inventory-receive-row').remove()}};document.getElementById('inventoryReceiveEnhanced').onsubmit=async e=>{e.preventDefault();const status=document.getElementById('irStatus'),submit=document.getElementById('irSubmit');const entries=[...rows.querySelectorAll('.inventory-receive-row')].map(r=>({productId:r.querySelector('.ir-product').value,quantity:r.querySelector('.ir-qty').value})).filter(x=>x.productId&&Number(x.quantity)>0);if(!entries.length){status.textContent='Select at least one product and enter a quantity.';status.classList.remove('hidden');return}submit.disabled=true;status.textContent='Receiving '+entries.length+' product line(s)…';status.classList.remove('hidden');try{const ref=document.getElementById('irReference').value,notes=document.getElementById('irNotes').value;for(const x of entries)await receive(x.productId,x.quantity,ref,notes);status.textContent='Stock received successfully for '+entries.length+' product line(s).';setTimeout(()=>{modal.classList.add('hidden');sync()},500)}catch(err){status.textContent='Receipt failed: '+err.message;submit.disabled=false}}}finally{modalBusy=false}}async function sync(){if(!route()||loading)return;loading=true;try{const ps=await products();if(!ps.length)return;const out=ps.filter(x=>qty(x)<=0).length,lowCount=ps.filter(x=>qty(x)>0&&low(x)>0&&qty(x)<=low(x)).length,value=ps.reduce((s,x)=>s+qty(x)*Number(x.cost_price??x.costPrice??x.cost??0),0);put('products',String(ps.length));put('stock lines',String(ps.length));put('low stock',String(lowCount));put('out of stock',String(out));put('stock value','KES '+value.toLocaleString('en-KE',{minimumFractionDigits:2,maximumFractionDigits:2}));table(ps);await enhanceReceive()}finally{loading=false}}window.addEventListener('hashchange',()=>setTimeout(sync,100));window.addEventListener('load',()=>setTimeout(sync,500));document.addEventListener('change',e=>{if(e.target?.id==='branchSelect')setTimeout(sync,100)});new MutationObserver(()=>{if(route()){setTimeout(sync,50);setTimeout(enhanceReceive,100)}}).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',()=>setTimeout(enhanceReceive,100),true);setInterval(()=>{sync();enhanceReceive()},1000)})();