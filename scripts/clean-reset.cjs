'use strict';
const { Pool } = require('pg');
const { parseAndValidateDatabaseUrl, railwaySsl } = require('./database-url.cjs');
const CONFIRM='DESTROY_ALL_POS_DATA_AND_REBUILD';
async function run(){if(process.env.RESET_CONFIRM!==CONFIRM)throw Error('Reset confirmation required');const {databaseUrl}=parseAndValidateDatabaseUrl('clean-reset');const pool=new Pool({connectionString:databaseUrl,ssl:railwaySsl(databaseUrl)});const c=await pool.connect();try{await c.query('BEGIN');const {rows}=await c.query("SELECT tablename FROM pg_tables WHERE schemaname='public'");for(const r of rows)await c.query('DROP TABLE IF EXISTS "'+String(r.tablename).replaceAll('"','""')+'" CASCADE');await c.query(`CREATE TABLE businesses(id BIGSERIAL PRIMARY KEY,name TEXT NOT NULL,phone TEXT,email TEXT,address TEXT,tax_number TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE branches(id BIGSERIAL PRIMARY KEY,business_id BIGINT NOT NULL REFERENCES businesses(id) ON DELETE CASCADE,name TEXT NOT NULL,code TEXT NOT NULL UNIQUE,address TEXT,phone TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE users(id BIGSERIAL PRIMARY KEY,branch_id BIGINT REFERENCES branches(id) ON DELETE SET NULL,name TEXT NOT NULL,email TEXT UNIQUE,password_hash TEXT NOT NULL,role TEXT NOT NULL DEFAULT 'cashier',active BOOLEAN NOT NULL DEFAULT true,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE products(id BIGSERIAL PRIMARY KEY,sku TEXT NOT NULL UNIQUE,barcode TEXT UNIQUE,name TEXT NOT NULL,description TEXT,cost_price NUMERIC(14,2) NOT NULL DEFAULT 0,selling_price NUMERIC(14,2) NOT NULL DEFAULT 0,active BOOLEAN NOT NULL DEFAULT true,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE stock_movements(id BIGSERIAL PRIMARY KEY,branch_id BIGINT NOT NULL REFERENCES branches(id),product_id BIGINT NOT NULL REFERENCES products(id),movement_type TEXT NOT NULL CHECK(movement_type IN('opening','receive','sale','adjustment','return')),quantity_delta NUMERIC(14,3) NOT NULL,unit_cost NUMERIC(14,2),reference_type TEXT,reference_id TEXT,note TEXT,created_by BIGINT REFERENCES users(id),created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE customers(id BIGSERIAL PRIMARY KEY,name TEXT NOT NULL,phone TEXT,email TEXT,address TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE sales(id BIGSERIAL PRIMARY KEY,branch_id BIGINT NOT NULL REFERENCES branches(id),customer_id BIGINT REFERENCES customers(id),sale_number TEXT NOT NULL UNIQUE,status TEXT NOT NULL DEFAULT 'completed',subtotal NUMERIC(14,2) NOT NULL DEFAULT 0,discount NUMERIC(14,2) NOT NULL DEFAULT 0,tax NUMERIC(14,2) NOT NULL DEFAULT 0,total NUMERIC(14,2) NOT NULL DEFAULT 0,created_by BIGINT REFERENCES users(id),created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE sale_items(id BIGSERIAL PRIMARY KEY,sale_id BIGINT NOT NULL REFERENCES sales(id) ON DELETE CASCADE,product_id BIGINT NOT NULL REFERENCES products(id),quantity NUMERIC(14,3) NOT NULL,unit_price NUMERIC(14,2) NOT NULL,discount NUMERIC(14,2) NOT NULL DEFAULT 0,line_total NUMERIC(14,2) NOT NULL);CREATE TABLE payments(id BIGSERIAL PRIMARY KEY,sale_id BIGINT NOT NULL REFERENCES sales(id) ON DELETE CASCADE,method TEXT NOT NULL,amount NUMERIC(14,2) NOT NULL,reference TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE quotations(id BIGSERIAL PRIMARY KEY,branch_id BIGINT NOT NULL REFERENCES branches(id),customer_id BIGINT REFERENCES customers(id),quotation_number TEXT NOT NULL UNIQUE,status TEXT NOT NULL DEFAULT 'draft',subtotal NUMERIC(14,2) NOT NULL DEFAULT 0,discount NUMERIC(14,2) NOT NULL DEFAULT 0,tax NUMERIC(14,2) NOT NULL DEFAULT 0,total NUMERIC(14,2) NOT NULL DEFAULT 0,notes TEXT,valid_until DATE,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE quotation_items(id BIGSERIAL PRIMARY KEY,quotation_id BIGINT NOT NULL REFERENCES quotations(id) ON DELETE CASCADE,product_id BIGINT REFERENCES products(id),description TEXT NOT NULL,quantity NUMERIC(14,3) NOT NULL,unit_price NUMERIC(14,2) NOT NULL,discount NUMERIC(14,2) NOT NULL DEFAULT 0,line_total NUMERIC(14,2) NOT NULL);CREATE TABLE invoices(id BIGSERIAL PRIMARY KEY,branch_id BIGINT NOT NULL REFERENCES branches(id),customer_id BIGINT REFERENCES customers(id),invoice_number TEXT NOT NULL UNIQUE,status TEXT NOT NULL DEFAULT 'issued',subtotal NUMERIC(14,2) NOT NULL DEFAULT 0,discount NUMERIC(14,2) NOT NULL DEFAULT 0,tax NUMERIC(14,2) NOT NULL DEFAULT 0,total NUMERIC(14,2) NOT NULL DEFAULT 0,notes TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE invoice_items(id BIGSERIAL PRIMARY KEY,invoice_id BIGINT NOT NULL REFERENCES invoices(id) ON DELETE CASCADE,product_id BIGINT REFERENCES products(id),description TEXT NOT NULL,quantity NUMERIC(14,3) NOT NULL,unit_price NUMERIC(14,2) NOT NULL,discount NUMERIC(14,2) NOT NULL DEFAULT 0,line_total NUMERIC(14,2) NOT NULL);CREATE INDEX idx_stock_branch_product ON stock_movements(branch_id,product_id,created_at);CREATE INDEX idx_sales_branch_created ON sales(branch_id,created_at);`);await c.query('COMMIT');console.log('clean reset complete')}catch(e){await c.query('ROLLBACK').catch(()=>{});throw e}finally{c.release();await pool.end()}}run().catch(e=>{console.error(e);process.exit(1)});
