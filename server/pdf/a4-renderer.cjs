'use strict';

/* Authoritative A4 invoice / quotation renderer. */
const fs = require('node:fs');
const path = require('node:path');
const PDFDocument = require('pdfkit');
const SVGtoPDF = require('@leduard/svg-to-pdfkit');
const bwipjs = require('bwip-js');
const BRAND = require('../document-branding.cjs');
const { validateDocument, normalizeDocument } = require('./schema.cjs');
const { moneyFromInput, mulCents, taxCents, formatMoney, formatNumber } = require('./format');
const { registerFonts } = require('./fonts.cjs');
const { adaptDocumentPayload } = require('./document-adapter.cjs');

const A4 = { width: 595.28, height: 841.89 };
const M = 40;
const W = A4.width - M * 2;
const FOOTER_Y = A4.height - 31;
const MAX_LOGO = 2 * 1024 * 1024;
const C = BRAND.colors;
const COL = { index: 34, item: 238, qty: 55, unit: 105, amount: W - 34 - 238 - 55 - 105 };

function text(pdf, value, x, y, o = {}) { pdf.font(o.font || 'body').fontSize(o.size || 9).fillColor(o.color || C.ink).text(String(value ?? ''), x, y, { width: o.width, align: o.align || 'left', lineGap: o.lineGap || 0, characterSpacing: o.characterSpacing || 0, continued: false }); }
function right(pdf, value, x, y, width, o = {}) { text(pdf, value, x, y, { ...o, width, align: 'right' }); }
function measured(pdf, value, width, size = 9, font = 'body') { pdf.font(font).fontSize(size); return pdf.heightOfString(String(value ?? ''), { width, lineGap: 0 }); }
function wrap(value) { return String(value ?? '').replace(/([^\s]{28})(?=[^\s])/g, '$1\u200b'); }
function raster(v) { return Buffer.isBuffer(v) && v.length > 4 && ((v[0] === 137 && v[1] === 80 && v[2] === 78 && v[3] === 71) || (v[0] === 255 && v[1] === 216 && v[2] === 255)); }
function svg(v) { const s = Buffer.isBuffer(v) ? v.toString('utf8') : String(v || ''); return /^\s*<svg(?:\s|>)/i.test(s) && Buffer.byteLength(s, 'utf8') <= MAX_LOGO; }
function decodeDataImage(value) { const s = String(value || '').trim(); let m = s.match(/^data:image\/(png|jpe?g);base64,(.+)$/i); if (m) { try { const b = Buffer.from(m[2], 'base64'); return raster(b) && b.length <= MAX_LOGO ? b : null; } catch (_) { return null; } } m = s.match(/^data:image\/svg\+xml(?:;charset=[^;]+)?;base64,(.+)$/i); if (m) { try { const b = Buffer.from(m[1], 'base64'); return svg(b) ? b.toString('utf8') : null; } catch (_) { return null; } } return null; }
async function loadLogo(source) { if (raster(source) || svg(source)) return source; const data = decodeDataImage(source); if (data) return data; const raw = String(source || '').trim(); if (!raw) return null; if (/^(iVBOR|\/9j\/)/.test(raw)) { try { const b = Buffer.from(raw, 'base64'); if (raster(b) && b.length <= MAX_LOGO) return b; } catch (_) {} } const files = []; if (raw.startsWith('/')) files.push(path.join(process.cwd(), 'public', raw.slice(1)), path.join(process.cwd(), raw.slice(1))); else if (!/^https?:\/\//i.test(raw)) files.push(path.resolve(process.cwd(), raw), path.join(process.cwd(), 'public', raw)); for (const file of files) { try { const b = fs.readFileSync(file); if (b.length <= MAX_LOGO && (raster(b) || svg(b))) return raster(b) ? b : b.toString('utf8'); } catch (_) {} } if (!/^https?:\/\//i.test(raw)) return null; try { const r = await fetch(raw, { redirect: 'follow' }); if (!r.ok) return null; const b = Buffer.from(await r.arrayBuffer()); if (b.length > MAX_LOGO) return null; if (raster(b)) return b; if (svg(b)) return b.toString('utf8'); } catch (_) {} return null; }
function drawLogo(pdf, x, y, size, logo) { if (!logo) throw new Error('Branding logo missing at render time'); pdf.save().strokeColor(C.orangeSoft).lineWidth(.7).roundedRect(x, y, size, size, 7).stroke(); try { if (typeof logo === 'string' && svg(logo)) SVGtoPDF(pdf, logo, x + 3, y + 3, { width: size - 6, height: size - 6, preserveAspectRatio: 'xMidYMid meet' }); else pdf.image(logo, x + 3, y + 3, { fit: [size - 6, size - 6], align: 'center', valign: 'center' }); } catch (e) { pdf.restore(); throw new Error(`Branding logo render failed: ${e?.message || e}`); } pdf.restore(); }
function drawTopBar(pdf) { const h = 4.5, widths = [W * .45, W * .35, W * .20]; let x = M; [C.ink, C.ink2, C.orange].forEach((c, i) => { pdf.fillColor(c).rect(x, 0, widths[i], h).fill(); x += widths[i]; }); }
function displayDate(value) { if (!value) return '—'; const m = String(value).match(/^(\d{4})-(\d{2})-(\d{2})$/); if (!m) return String(value); return new Intl.DateTimeFormat('en-GB', { day: '2-digit', month: 'short', year: 'numeric', timeZone: 'UTC' }).format(new Date(Date.UTC(+m[1], +m[2] - 1, +m[3]))); }
function lineTotal(item) { const gross = mulCents(moneyFromInput(item.unitPrice), item.qty); const discount = moneyFromInput(item.discount || '0'); const net = Math.max(0, gross - discount); const tax = taxCents(net, item.taxRate || 0); return { gross, discount, tax, total: net + tax }; }
function totals(items) { return items.reduce((o, item) => { const t = lineTotal(item); o.subtotal += t.gross; o.discount += t.discount; o.tax += t.tax; o.total += t.total; return o; }, { subtotal: 0, discount: 0, tax: 0, total: 0 }); }
function drawHeader(pdf, doc, logo) { drawTopBar(pdf); const top = 34, logoSize = 48, brandX = M + logoSize + 14, metaW = 178, metaX = A4.width - M - metaW, brandW = Math.max(170, metaX - brandX - 22); drawLogo(pdf, M, top, logoSize, logo); let brandY = top - 1; const name = String(doc.company.name || BRAND.legalName); text(pdf, name, brandX, brandY, { font: 'bold', size: 14.25, width: brandW }); brandY += Math.max(18, measured(pdf, name, brandW, 14.25, 'bold')) + 2; text(pdf, doc.company.tagline || BRAND.tagline, brandX, brandY, { font: 'bold', size: 7.8, color: C.orangeDeep, width: brandW, characterSpacing: .65 }); brandY += 12; for (const value of [doc.company.website || BRAND.website, doc.company.address || BRAND.address, doc.company.phone || BRAND.phone, doc.company.email ? `Email: ${doc.company.email}` : '', doc.company.taxId ? `PIN: ${doc.company.taxId}` : ''].filter(Boolean)) { text(pdf, value, brandX, brandY, { size: 8.05, color: C.muted, width: brandW }); brandY += measured(pdf, value, brandW, 8.05) + 1.5; } text(pdf, doc.type === 'quotation' ? 'Quotation' : 'Invoice', metaX, top - 2, { font: 'bold', size: 20, width: metaW, align: 'right' }); right(pdf, `No. ${doc.number}`, metaX, top + 24, metaW, { size: 9.1, color: C.muted }); right(pdf, `Issued    ${displayDate(doc.date)}`, metaX, top + 42, metaW, { size: 8.4, color: C.muted }); right(pdf, `${doc.type === 'quotation' ? 'Valid until' : 'Due'}    ${displayDate(doc.type === 'quotation' ? doc.validUntil : doc.dueDate)}`, metaX, top + 56, metaW, { size: 8.4, color: C.muted }); if (doc.servedBy) right(pdf, `Served by    ${doc.servedBy}`, metaX, top + 70, metaW, { size: 8.2, color: C.muted }); const bottom = Math.max(brandY, top + 86) + 15; pdf.strokeColor(C.ink).lineWidth(1.1).moveTo(M, bottom).lineTo(A4.width - M, bottom).stroke(); return bottom + 1; }
function drawParties(pdf, doc, y) { const gap = 24, colW = (W - gap) / 2, leftX = M, rightX = M + colW + gap; text(pdf, 'BILLED TO', leftX, y, { font: 'bold', size: 7.4, color: C.muted, width: colW, characterSpacing: .8 }); text(pdf, doc.customer.name, leftX, y + 16, { font: 'bold', size: 10.6, width: colW }); let leftY = y + 16 + measured(pdf, doc.customer.name, colW, 10.6, 'bold') + 4; for (const value of [doc.customer.address, doc.customer.phone, doc.customer.email, doc.customer.taxId ? `PIN: ${doc.customer.taxId}` : ''].filter(Boolean)) { text(pdf, value, leftX, leftY, { size: 8.5, color: C.muted, width: colW }); leftY += measured(pdf, value, colW, 8.5) + 2; } text(pdf, 'ORDER REFERENCE', rightX, y, { font: 'bold', size: 7.4, color: C.muted, width: colW, characterSpacing: .8 }); const ref = doc.orderReference || '—'; text(pdf, ref, rightX, y + 16, { font: 'bold', size: 10.6, width: colW }); let rightY = y + 16 + measured(pdf, ref, colW, 10.6, 'bold') + 4; for (const value of [doc.channel ? `Channel: ${doc.channel}` : '', doc.paymentMethod ? `Payment method: ${doc.paymentMethod}` : '', `Status: ${doc.status || (doc.type === 'quotation' ? 'Pending approval' : 'Awaiting payment')}`].filter(Boolean)) { text(pdf, value, rightX, rightY, { size: 8.5, color: C.muted, width: colW }); rightY += measured(pdf, value, colW, 8.5) + 2; } const bottom = Math.max(leftY, rightY) + 12; pdf.strokeColor(C.lineSoft).lineWidth(.7).moveTo(M, bottom).lineTo(A4.width - M, bottom).stroke(); return bottom + 1; }
function drawTableHeader(pdf, y) { const h = 22; let x = M; for (const [label, width, numeric] of [['#', COL.index, false], ['Item', COL.item, false], ['Qty', COL.qty, true], ['Unit price', COL.unit, true], ['Amount', COL.amount, true]]) { if (numeric) right(pdf, label, x, y, width, { font: 'bold', size: 7.4, color: C.muted }); else text(pdf, label, x, y, { font: 'bold', size: 7.4, color: C.muted, width }); x += width; } pdf.strokeColor(C.ink).lineWidth(1).moveTo(M, y + h).lineTo(A4.width - M, y + h).stroke(); return y + h + 1; }
function itemHeight(pdf, item) { const w = COL.item - 12; const nameH = measured(pdf, wrap(item.description), w, 9.1, 'bold'); const subH = item.sub ? measured(pdf, wrap(item.sub), w, 7.8) : 0; return Math.max(31, nameH + subH + 15); }
function drawItem(pdf, item, y, index, currency, last = false) { const h = itemHeight(pdf, item); let x = M; text(pdf, String(index + 1).padStart(2, '0'), x + 4, y + 8, { font: 'bold', size: 7.8, color: C.orangeDeep, width: COL.index - 8 }); x += COL.index; const w = COL.item - 12, nameH = measured(pdf, wrap(item.description), w, 9.1, 'bold'); text(pdf, wrap(item.description), x + 4, y + 7, { font: 'bold', size: 9.1, width: w }); if (item.sub) text(pdf, wrap(item.sub), x + 4, y + 7 + nameH + 2, { size: 7.8, color: C.muted, width: w }); x += COL.item; right(pdf, formatNumber(item.qty), x, y + 8, COL.qty - 7, { size: 8.5 }); x += COL.qty; right(pdf, formatMoney(moneyFromInput(item.unitPrice), currency), x, y + 8, COL.unit - 7, { size: 8.5 }); x += COL.unit; right(pdf, formatMoney(lineTotal(item).total, currency), x, y + 8, COL.amount - 7, { font: 'bold', size: 8.5 }); pdf.strokeColor(last ? C.ink : C.lineSoft).lineWidth(last ? 1 : .55).moveTo(M, y + h).lineTo(A4.width - M, y + h).stroke(); return y + h; }
function finalMetrics(pdf, doc, values) { const totalsH = (values.discount ? 3 : 2) * 17 + 39; const note = doc.terms ? `${doc.notes || ''}\n${doc.terms}` : (doc.notes || (doc.type === 'quotation' ? 'This quotation is valid for 14 days from the issue date. Prices are subject to stock availability at time of order confirmation.' : 'Goods once sold are exchangeable within 7 days with receipt. Prices include VAT where applicable. Thank you for shopping with us.')); const leftH = measured(pdf, note, 285, 8.3) + 40; const p = doc.paymentDetails || {}; const rows = [['M-Pesa Paybill', p.paybill], ['Till', p.till], ['Account', p.account], ['Bank', p.bank]].filter(([, v]) => v).length; const footerH = Math.max(leftH, 35 + rows * 17) + 13; return { reserve: 18 + totalsH + 15 + footerH + 8 + 52 + 8 }; }
function drawTotals(pdf, doc, values, y) { const width = 210, x = A4.width - M - width; let cy = y; const line = (label, value, color = C.ink) => { text(pdf, label, x, cy, { size: 8.4, color: C.muted, width: width - 95 }); right(pdf, value, x + 90, cy, width - 90, { size: 8.4, color }); cy += 17; }; line('Subtotal', formatMoney(values.subtotal, doc.currency)); line(`VAT (${doc.items.length ? Math.round((values.tax / Math.max(1, values.subtotal - values.discount)) * 100) : 0}%)`, formatMoney(values.tax, doc.currency)); if (values.discount) line('Discount', `- ${formatMoney(values.discount, doc.currency)}`, C.danger); const label = doc.type === 'quotation' ? 'Estimated total' : 'Total due'; pdf.fillColor(C.ink).roundedRect(x, cy + 1, width, 36, 6).fill(); pdf.fillColor(C.orange).rect(x, cy + 1, 4, 36).fill(); text(pdf, label, x + 13, cy + 12, { font: 'bold', size: 8.7, color: C.white, width: 95 }); right(pdf, formatMoney(values.total, doc.currency), x + 92, cy + 10, width - 105, { font: 'bold', size: 12.5, color: C.white }); return cy + 39; }
function drawFooter(pdf, doc, y, qr) { const leftW = 285, gap = 28, rightX = M + leftW + gap, rightW = W - leftW - gap; const note = doc.terms ? `${doc.notes || ''}\n${doc.terms}` : (doc.notes || (doc.type === 'quotation' ? 'This quotation is valid for 14 days from the issue date. Prices are subject to stock availability at time of order confirmation.' : 'Goods once sold are exchangeable within 7 days with receipt. Prices include VAT where applicable. Thank you for shopping with us.')); const p = doc.paymentDetails || {}; const rows = [['M-Pesa Paybill', p.paybill], ['Till', p.till], ['Account', p.account], ['Bank', p.bank]].filter(([, v]) => v); const height = Math.max(measured(pdf, note, leftW, 8.3) + 40, 35 + rows.length * 17 + (qr ? 70 : 0)); pdf.strokeColor(C.lineSoft).lineWidth(.7).moveTo(M, y).lineTo(A4.width - M, y).stroke(); text(pdf, 'NOTES & TERMS', M, y + 12, { font: 'bold', size: 7.4, color: C.muted, width: leftW }); text(pdf, note, M, y + 27, { size: 8.3, width: leftW, lineGap: 2 }); text(pdf, 'PAYMENT DETAILS', rightX, y + 12, { font: 'bold', size: 7.4, color: C.muted, width: rightW }); let py = y + 28; for (const [label, value] of rows) { text(pdf, label, rightX, py, { size: 8.1, color: C.muted, width: rightW * .52 }); right(pdf, value, rightX + rightW * .48, py, rightW * .52, { size: 8.1 }); py += 17; } if (qr) { pdf.image(qr, rightX, py + 5, { fit: [56, 56] }); text(pdf, 'Scan to visit uniquesolarltd.co.ke', rightX + 64, py + 21, { size: 7.1, color: C.muted, width: rightW - 64 }); } return y + height + 13; }
function drawSignatures(pdf, doc, y) { const gap = 24, width = (W - gap) / 2, lineY = y + 30; pdf.strokeColor(C.ink).lineWidth(.65).moveTo(M, lineY).lineTo(M + width, lineY).stroke(); pdf.moveTo(M + width + gap, lineY).lineTo(A4.width - M, lineY).stroke(); text(pdf, doc.preparedBy ? `Prepared by: ${doc.preparedBy}` : 'Prepared by', M, lineY + 6, { size: 7.5, color: C.muted, width, align: 'center' }); text(pdf, doc.customerAcknowledgement || 'Customer acknowledgement', M + width + gap, lineY + 6, { size: 7.5, color: C.muted, width, align: 'center' }); }
function drawPageFooter(pdf, page, count) { pdf.strokeColor(C.lineSoft).lineWidth(.6).moveTo(M, FOOTER_Y - 9).lineTo(A4.width - M, FOOTER_Y - 9).stroke(); text(pdf, `${BRAND.legalName}  ·  ${BRAND.website.replace(/^https?:\/\//, '')}`, M, FOOTER_Y, { size: 6.8, color: C.muted, width: W - 110 }); right(pdf, `Page ${page} of ${count}`, A4.width - M - 90, FOOTER_Y, 90, { size: 6.8, color: C.muted }); }
function drawContinuationHeader(pdf, doc, logo) { drawTopBar(pdf); drawLogo(pdf, M, 20, 30, logo); text(pdf, doc.type === 'quotation' ? 'Quotation' : 'Invoice', M + 40, 24, { font: 'bold', size: 12, width: 180 }); right(pdf, `No. ${doc.number}`, A4.width - M - 180, 25, 180, { size: 8.2, color: C.muted }); pdf.strokeColor(C.line).lineWidth(.8).moveTo(M, 57).lineTo(A4.width - M, 57).stroke(); }
function takeRows(rows, cap) { const out = []; let used = 0; for (const row of rows) { if (out.length && used + row.height > cap) break; out.push(row); used += row.height; } if (!out.length && rows.length) out.push(rows[0]); return out; }
async function makeQrBuffer(url) { try { return await bwipjs.toBuffer({ bcid: 'qrcode', text: String(url || ''), scale: 3, padding: 0, includetext: false }); } catch (_) { return null; } }

async function renderDocument({ type, doc: input, company }) {
  if (!company || typeof company !== 'object') throw new Error('company: must be an object');
  const requested = company.logo || company.logoUrl || company.logo_url || company.logoPath || company.logo_path || BRAND.logo;
  const logo = (await loadLogo(requested)) || (await loadLogo(BRAND.logo)); if (!logo) throw new Error('Branding logo could not be loaded; refusing to generate an unbranded customer document');
  const normalizedCompany = { ...company, logo }; validateDocument(type, input, normalizedCompany); const doc = normalizeDocument(type, input, normalizedCompany); const calculated = totals(doc.items); const qr = await makeQrBuffer(BRAND.website);
  const probe = new PDFDocument({ size: 'A4' }); registerFonts(probe); const headerY = drawHeader(probe, doc, logo); const partiesY = drawParties(probe, doc, headerY + 16); const tableY = drawTableHeader(probe, partiesY + 16) + 1; const metrics = finalMetrics(probe, doc, calculated); const rows = doc.items.map((item, index) => ({ item, index, height: itemHeight(probe, item) })); probe.end();
  const sum = rows.reduce((s, r) => s + r.height, 0), firstOpen = Math.max(40, FOOTER_Y - tableY - 12), firstFinal = firstOpen - metrics.reserve, contTableY = 79, contOpen = FOOTER_Y - contTableY - 12, contFinal = contOpen - metrics.reserve; const pages = []; let remaining = rows;
  if (sum <= firstFinal) { pages.push({ kind: 'first-final', rows: remaining }); remaining = []; } else { const first = takeRows(remaining, firstOpen); pages.push({ kind: 'first', rows: first }); remaining = remaining.slice(first.length); while (remaining.length) { const rest = remaining.reduce((s, r) => s + r.height, 0); if (rest <= contFinal) { pages.push({ kind: 'continuation-final', rows: remaining }); remaining = []; break; } const chunk = takeRows(remaining, contOpen); pages.push({ kind: 'continuation', rows: chunk }); remaining = remaining.slice(chunk.length); } if (!pages.some(p => p.kind.endsWith('final'))) pages.push({ kind: 'continuation-final', rows: [] }); }
  const pdf = new PDFDocument({ size: 'A4', autoFirstPage: false, bufferPages: true, margins: { top: M, bottom: M, left: M, right: M }, info: { Title: `${type === 'quotation' ? 'Quotation' : 'Invoice'} ${doc.number}`, Author: BRAND.legalName } }); registerFonts(pdf); const chunks = []; const done = new Promise((resolve, reject) => { pdf.on('data', c => chunks.push(c)); pdf.once('end', () => resolve(Buffer.concat(chunks))); pdf.once('error', reject); });
  pages.forEach(page => { pdf.addPage({ size: 'A4', margins: { top: M, bottom: M, left: M, right: M } }); let y; if (page.kind === 'first' || page.kind === 'first-final') { y = drawHeader(pdf, doc, logo); y = drawParties(pdf, doc, y + 16); y = drawTableHeader(pdf, y + 16) + 1; } else { drawContinuationHeader(pdf, doc, logo); y = drawTableHeader(pdf, contTableY) + 1; } page.rows.forEach((row, i) => { y = drawItem(pdf, row.item, y, row.index, doc.currency, i === page.rows.length - 1); }); if (page.kind.endsWith('final')) { y += 18; y = drawTotals(pdf, doc, calculated, y) + 15; y = drawFooter(pdf, doc, y, qr) + 8; drawSignatures(pdf, doc, y); } });
  const range = pdf.bufferedPageRange(); for (let i = range.start; i < range.start + range.count; i += 1) { pdf.switchToPage(i); drawPageFooter(pdf, i + 1, range.count); } pdf.end(); return done;
}
async function renderPdfBuffer(payload, paper = 'a4') { const adapted = adaptDocumentPayload(payload, paper); if (adapted.type !== 'invoice' && adapted.type !== 'quotation') throw new Error(`Unsupported PDF document type: ${adapted.type}`); return renderDocument({ type: adapted.type, doc: adapted.doc, company: adapted.company }); }
module.exports = { renderDocument, renderPdfBuffer, loadLogo };
