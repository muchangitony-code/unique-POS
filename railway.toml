[build]
buildCommand = "npm install --omit=dev && npm run build"

[deploy]
startCommand = "node index.runtime.cjs"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
